package sf.sequence;

/**
 * 串是开发中非常熟悉的字符串,是由若干个字符组成的有限序列
 *
 * 字符串
 * 前缀(prefix). 可包含整个字符串
 * 真前缀(proper prefix). 不包含整个字符串
 * 后缀(suffix). 可包含整个字符串
 * 真后缀(proper suffix) . 不包含整个字符串
 *
 * 串匹配算法
 *
 * 查找一个模式串(Pattern)在文本串(Text)中的位置. [研究如何indexOf(pattern)更高效]
 * 1.蛮力算法(Brute Force)
 * 2.KMP
 * 3.Boyer-Moore
 * 4.Rabin-Karp
 * 5.Sunday
 * tlen代表文本串 Text的长度,plen代表模式串Pattern的长度
 *
 *
 */
public class Sequence {



}
